export const SYMBOL = Object.freeze({
  comma: ',',
  dash: '-',
});

export const UNIT = Object.freeze({
  count: '개',
  won: '원',
});

export const STR_DAY = Object.freeze({
  week: 'week',
  weekend: 'weekend',
});